t=(2,3,4,5,6)
print(t[0])


#We cannot update the value of its indices. & It uses round brackets
# It does not support item assignment.
# It is an immutable datatype.



#a=() = Empty
#a=(1,) = Tuple with one element. Python Interpreter won't be able to recognize that it is a tuple if we will not use comma.
#a=(2,3,5) = Tuple with more than one element





